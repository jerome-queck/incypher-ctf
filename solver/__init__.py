"""The Solver — the autonomous CTF agent this repository ships as a container.

Standard library only, everywhere under here. The image installs an interpreter and the Category
tooling and nothing else, and the competition run is unattended, so a missing dependency is a
failure with nobody present to fix it (`CODING_STANDARDS.md` §6).
"""
