"""The Solver — the autonomous CTF agent this repository ships as a container.

Standard library except the exact Candidate vault's immutable, build-probed AES-GCM dependency.
The competition run is unattended, so any undeclared or runtime-installed dependency is a failure
with nobody present to fix it (`CODING_STANDARDS.md` §6).
"""
